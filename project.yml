name: FitTrack
options:
  bundleIdPrefix: com.henrique
targets:
  FitTrack:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - App
      - Data
      - Models
      - Services
      - Views
    info:
      path: Info.plist
      properties:
        UILaunchScreen: {}
        CFBundleExecutable: FitTrack
        CFBundleIdentifier: com.henrique.FitTrack
